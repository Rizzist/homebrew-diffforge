# Homebrew tap for Diff Forge AI

Install the `diffforge` CLI:

```sh
brew install rizzist/diffforge/diffforge
```

Formulae in this tap are updated automatically by the
[diffforge-cli](https://github.com/Rizzist/diffforge-cli) release workflow.
